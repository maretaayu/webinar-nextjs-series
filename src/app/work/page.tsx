import { generateMetadata } from "@/utils/metadata";

export const metadata = generateMetadata({
  title: "Work page",
  description: "ini work page",
});

function Work() {
  return (
    <div>
      <h1>ini halaman work</h1>
    </div>
  );
}

export default Work;
